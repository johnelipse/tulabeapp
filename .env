TULABE_BASE_API=https://tulabe-api.lectify.education/api

# Push relay (see relay/ in this repo). Change PUSH_RELAY_URL when the relay
# is deployed somewhere reachable (e.g. https://your-relay.example).
# The current value is the PC's LAN address for on-device testing.
PUSH_RELAY_URL=http://192.168.100.41:8787
PUSH_RELAY_KEY=